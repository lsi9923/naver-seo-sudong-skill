---
name: naver-seo-sudong-skill
description: 네이버쇼핑(스마트스토어) 상위노출 최적화 수동 상품등록 전문 스킬. "네이버 등록해줘", "네이버 등록", "네이버 상품등록", "이 유알엘", "이 URL", "상품등록", "상세페이지 분석" 등의 요청 시 1688/타오바오/도매 상품 URL을 분석하여 전용 Chrome 자동 실행 -> 카테고리 relevance 1.0 실측 확정 -> 네이버 공식 연관검색어 및 terms 추출 -> 25~30자 상품명 설계 및 terms 검증 -> 스마트스토어 폼 20개 전 항목을 복붙 가능한 완성형으로 출력한다.
---

# 네이버쇼핑 상위노출 수동등록 (적합도·인기도·신뢰도 실전 프로토콜)

출처: 최이호 「온라인 진실의 방」 강의 정리본 기반 자동 실측 파이프라인.  
사용자가 **"네이버 등록해줘 이 URL"**, **"네이버 상품등록"**, **"상품등록"** 등을 요청하면 이 스킬의 순서대로 즉시 진행한다.

---

## 0. 알고리즘 대원칙 (판단의 기준선)

```text
최종 랭킹 = [적합도 (진입 장벽)] + [인기도 (순위 결정력)] − [신뢰도 감점]
```

1. **GBDT는 컷오프 필터다.** 적합도가 기준치 미만이면 클릭·판매량이 아무리 높아도 상위 노출에서 완전히 잘린다.
2. **적합도가 0이면 인기도 100만 점도 1페이지 진입 불가.** (카테고리 1.0 만점 진입이 최우선)
3. **적합도 가중 서열:**  
   **카테고리(절대적) > 상품명(핵심) > 스토어명 > 브랜드/제조사 ≫ 속성·태그(미미), 이미지(가산점 0%)**
4. **상품명 5대 규칙 (25~30자 엄수):**
   - 완성형 키워드는 맨 왼쪽에 배치
   - 조합형 키워드는 간격을 좁게 유지
   - 조합형은 좌 → 우 순서 유지
   - 길이는 공백 포함 **25~30자 (최대 35자)** — 35자 초과 시 신뢰도/간소화 감점
   - 중복 키워드는 1회만 허용 (남발 금지)

---

## 1. 실행 파이프라인 (완전 자동화)

사용자가 상품 URL을 주면 질문하지 않고 다음을 즉시 자동 수행한다:

### 1단계: 상품 스펙 분석
- 대상 URL(1688, 타오바오 등)을 브라우저/DOM으로 분석하여 핵심 스펙(품목, 소재, 규격, 색상, 무게, 기능 등) 파악
- 핵심 후보 메인 키워드 1~2개 도출 (예: `자전거장갑`, `방한장갑`)

### 2단계: 크롬 자동 런치 & 실측 파이프라인 실행
스크립트 실행 시 Chrome CDP(`http://127.0.0.1:9222`)가 켜져 있지 않으면 전용 프로필(`Chrome Automation`)로 자동 실행된다.
```bash
python scripts/run_pipeline.py "<후보키워드>" --title "<설계상품명>"
```
- **카테고리 확정**: 네이버 검색엔진의 `relevance` 실측값이 1.0(만점)인 경로를 최종 확정.
- **공식 연관검색어 추출**: 네이버쇼핑 검색 결과의 `relatedQueries`를 실시간 수집.
- **형태소 색인어(Terms) 추출**: 검색엔진 인덱서의 `terms`, `categoryNNameIdxTerm` 추출.
- **스마트스토어 태그 10개 확정**: 연관검색어 중 태그 사전에 등록된 10개 자동 선정.

### 3단계: 상품명 설계 및 Terms 검증
- 메인 완성형 키워드를 좌측 0번 위치에 배치
- 추출된 공식 연관검색어 중 핵심 니즈 키워드 조합
- **공백 포함 25~30자 (최대 35자)**로 맞추고 `terms.py --title`로 검증 통과(✓) 확인

### 4단계: 스마트스토어 20개 항목 폼 출력
사용자가 그대로 복사해서 스마트스토어 센터에 붙여넣을 수 있도록 20개 항목을 채워 출력한다.

---

## 2. 판매자 기본 고정정보 (기본 셋팅)

- **출고지:** 인천광역시 검단구 완정로 146 (리더스빌) 2층 208-43c호 (23466)
- **반품/교환지:** 위와 동일
- **택배사:** CJ대한통운 / 한진택배 (선택)
- **부가세:** 과세상품
- **반품배송비(편도):** 3,500원 / **교환배송비(왕복):** 7,000원
- **A/S 안내:** 판매자 톡톡 및 고객센터 문의

---

## 3. 스마트스토어 등록 폼 출력 템플릿 (20개 항목)

1. **카테고리 (필수):** 대분류 > 중분류 > 소분류 > 세분류 (실측 relevance 근거 제시)
2. **예약구매:** 설정안함
3. **상품명 (필수):** [A안] 및 [B안] 제시, 글자 수 반드시 표기 (예: 26자)
4. **판매가 (필수):** 원가, 마진, 권장 판매가 계산
5. **재고수량 (필수):** 옵션 합계 수량
6. **옵션:** 단독형/조합형, 옵션명, 옵션값, 추가금액
7. **대표이미지/추가이미지:** 1000×1000 정사각형 권장 (중국어 문구 제거 필수 안내)
8. **상세설명:** 후킹 → 특장점 → 상세 스펙표 → 배송/교환 안내 구성안
9. **상품 주요정보:** 모델명, 브랜드(무브랜드), 제조사, 원산지(중국), 상품상태(신상품), KC인증 여부
10. **사이즈:** 해당 카테고리 규격
11. **상품정보제공고시:** 품목군 선택 및 법정 고시 항목
12. **배송:** 일반배송, 배송비 정책, 묶음배송 가능
13. **반품/교환:** 배송비, 반품지 주소
14. **A/S, 특이사항:** 안내 문구
15. **추가상품:** 함께 팔기 좋은 세트/소모품 제안
16. **구매/리뷰 혜택:** 리뷰 작성 포인트 지급 권장 (인기도 1,000개 수렴 지점 공략)
17. **검색설정 (태그 10개):** 실측 연관검색어 기반 10개
18. **SEO 설정:** Page Title / Meta Description
19. **노출채널:** 스마트스토어 체크, 네이버쇼핑 가격비교 등록 체크
20. **쇼핑 상품정보 검색품질 체크:** 저장 전 버튼 클릭 통과 확인 안내
